# ============================================================
# SIRI-AI JTM — Environment Configuration
# Backend: Supabase (PostgreSQL) per PRD §4.1 & §9
# ============================================================

# --- Database (Supabase Postgres direct connection) ---
# Used by Prisma for schema push, migrations, and runtime queries.
# NOTE: Replace [YOUR-PASSWORD] with your actual Supabase database password
#       (Project Settings → Database → Connection string → password)
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.wgdvqrpntonxodaawzol.supabase.co:5432/postgres"

# --- Supabase Project (per PRD §4.1, §17.1) ---
# Stored for reference / future Supabase client SDK integration.
# Currently SIRI-AI uses NextAuth + Prisma directly; Supabase Auth/Storage
# can be wired in a later phase per PRD §2.3.
SUPABASE_URL="https://wgdvqrpntonxodaawzol.supabase.co"
SUPABASE_ANON_KEY="sb_publishable__hLpIxuv0qv7FsYu-B2fJw_iIGlRvhF"
SUPABASE_PROJECT_REF="wgdvqrpntonxodaawzol"

# --- NextAuth (PRD §12 — JWT-based session, 8h expiry) ---
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="siri-ai-jtm-dev-secret-2026-not-for-production"

# --- Z.AI GLM (PRD §10 — server-side only, never exposed to client) ---
# Used by /api/ai/generate route via z-ai-web-dev-sdk.
# The SDK auto-discovers the key from environment; no manual env needed here.
